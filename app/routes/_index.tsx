import { Typography } from "@mui/material";

export default function Index() {
  return (
    <div>
      <Typography variant="h1">Remixtape</Typography>
      <Typography variant="h2">Make your own mixed tapes</Typography>
    </div>
  );
}
